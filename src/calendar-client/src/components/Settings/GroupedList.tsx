import { List } from "@mui/material";
import React, { FC } from "react";

interface Props {

}

export const GroupedList: FC<Props> = (props) => {
    const range
    return (

        <List sx={{
            width: '100%',
            maxWidth: 360,
            bgcolor: 'background.paper',
            position: 'relative',
            overflow: 'auto',
            maxHeight: 300,
            '& ul': {padding: 0},
        }}
              subheader={<li/>}>

        </List>
    )
}